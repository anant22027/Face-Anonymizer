import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Face Anonymizer - Privacy Protection Tool',
  description: 'Automatically detect and anonymize faces in photos and videos to protect privacy',
  keywords: 'face anonymizer, privacy, blur faces, video anonymization, photo privacy',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}